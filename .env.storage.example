# ===========================================
# 图片存储配置示例
# ===========================================
#
# 复制此文件内容到你的 .env 文件中
# 根据需要选择存储类型并配置相应参数
#

# -------------------------------------------
# 存储类型选择（必填）
# -------------------------------------------
# 可选值: local, minio, oss, s3
# - local: 本地文件系统（开发/测试）
# - minio: MinIO 对象存储（推荐生产环境）
# - oss: 阿里云 OSS（国内业务）
# - s3: AWS S3（全球业务）
STORAGE_TYPE=local


# ===========================================
# MinIO 配置（当 STORAGE_TYPE=minio 时使用）
# ===========================================

# MinIO 服务地址（不含协议）
MINIO_ENDPOINT=localhost:9000

# MinIO 访问密钥
MINIO_ACCESS_KEY=minioadmin

# MinIO 密钥
MINIO_SECRET_KEY=minioadmin123

# MinIO 存储桶名称
MINIO_BUCKET=product-warehouse

# 是否使用 HTTPS 连接 MinIO
# - false: 使用 HTTP（本地开发）
# - true: 使用 HTTPS（生产环境）
MINIO_SECURE=false

# MinIO 公开访问 URL（可选）
# 如果配置了反向代理或 CDN，请设置此项
# 示例: https://cdn.yourdomain.com
# 留空则使用默认: http://localhost:9000
MINIO_PUBLIC_URL=


# ===========================================
# 阿里云 OSS 配置（当 STORAGE_TYPE=oss 时使用）
# ===========================================

# 阿里云 AccessKey ID
OSS_ACCESS_KEY_ID=

# 阿里云 AccessKey Secret
OSS_ACCESS_KEY_SECRET=

# OSS 节点（Endpoint）
# 示例: oss-cn-hangzhou.aliyuncs.com
OSS_ENDPOINT=

# OSS 存储桶名称
OSS_BUCKET=

# CDN 加速域名（可选）
# 示例: https://cdn.yourdomain.com
OSS_CDN_DOMAIN=


# ===========================================
# AWS S3 配置（当 STORAGE_TYPE=s3 时使用）
# ===========================================
# 注意：需要安装 boto3: pip install boto3
# 配置方式类似 OSS，请参考 AWS 官方文档


# ===========================================
# 配置示例
# ===========================================

# ========== 示例 1: 本地存储（开发环境） ==========
# STORAGE_TYPE=local
# （无需其他配置）

# ========== 示例 2: MinIO（Docker 本地） ==========
# STORAGE_TYPE=minio
# MINIO_ENDPOINT=localhost:9000
# MINIO_ACCESS_KEY=minioadmin
# MINIO_SECRET_KEY=minioadmin123
# MINIO_BUCKET=product-warehouse
# MINIO_SECURE=false

# ========== 示例 3: MinIO（生产环境 + HTTPS） ==========
# STORAGE_TYPE=minio
# MINIO_ENDPOINT=minio.yourdomain.com:443
# MINIO_ACCESS_KEY=your_production_access_key
# MINIO_SECRET_KEY=your_production_secret_key
# MINIO_BUCKET=product-warehouse
# MINIO_SECURE=true
# MINIO_PUBLIC_URL=https://cdn.yourdomain.com

# ========== 示例 4: 阿里云 OSS（生产环境） ==========
# STORAGE_TYPE=oss
# OSS_ACCESS_KEY_ID=LTAI5t...
# OSS_ACCESS_KEY_SECRET=your_secret_key
# OSS_ENDPOINT=oss-cn-hangzhou.aliyuncs.com
# OSS_BUCKET=product-warehouse
# OSS_CDN_DOMAIN=https://cdn.yourdomain.com
